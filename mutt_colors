# vim: filetype=muttrc

# Header
color hdrdefault white default

# Specific headers
color header brightblue default "Subject: " 

# Attachment within the message
color attachment yellow default 

# Levels of quoted previous messages
color quoted green default 
color quoted1 cyan default 
color quoted2 green default 
color quoted3 cyan default 

# Messages
color error red default # error messages 
color message yellow default # message informational messages 

color indicator black yellow # indicator for the "current message" 

color status green default# status lines in the folder index sed for the mini-help line 


color tree cyan default # the "tree" display of threads within the folder index 

color search brightgreen default # search matches found with search within the internal pager 
color markers black default # The markers indicate a wrapped line 
color index brightyellow default '~N'
color index brightblue default '~F' # Flagged Messages are important! 
color index brightred default '~D' # Deleted Mails

# identifies email addresses 
color body green black [\-\.+_a-zA-Z0-9]+@[\-\.a-zA-Z0-9]+

# identifies URLs 
color body red black (https?|ftp)://[\-\.,/%~_:?&=\#a-zA-Z0-9]+
